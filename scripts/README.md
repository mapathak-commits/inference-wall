# Shared scripts

Server-launch scripts used across experiments.

| Script | Purpose |
|---|---|
| `start_server.sh` | Launch the vLLM server with the serving config under test |
| `start_server_prof.sh` | Same, with the torch profiler armed (for trace captures) |
